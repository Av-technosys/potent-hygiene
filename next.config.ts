import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["potent-hygiene.s3.amazonaws.com", "cdn.shopify.com", "dw0n4qiceose7.cloudfront.net", "potent-hygiene.s3.ap-south-1.amazonaws.com"],
  },
};

export default nextConfig;
